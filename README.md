# Audit Program Setup

## Requirements
Make sure Python is installed:
    python --version

Check that pip is installed:
    pip --version

## Install Needed Packages
Run this in Command Prompt:
    pip install pandas requests

## Run the Program
After downloading the folder, run:
    python auditprogram/main.py
